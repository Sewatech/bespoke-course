/* Margin and padding */
set-margin(class position max) 
  for index in (0..max) 
    {class}\.margin-{position}-{index} 
      margin-{position} 2.5rem * index
set-padding(position max) 
  for index in (0..max) 
    \.padding-{position}-{index} 
      padding-{position} 2.5rem * index
set-position(position max) 
  for index in (0..max) 
    .{position}-{index} 
      position absolute
      {position} 2.5rem * index
set-width(parent) 
  for index in (0..20) 
    {parent}\.width-{index*5} 
      width 5% * index
      margin-left: auto;
      margin-right: auto;

// Declare begore the margin
set-width('') 
set-width('pre.source') 

set-margin('',top, 20) 
set-margin('.uml',top, 20) 
set-margin('pre.source',top, 20) 
set-margin('.level2',top, 20) 
set-margin('',left, 20) 
set-margin('.uml',left, 20) 
set-margin('pre.source',left, 20) 
set-margin('',right, 20) 
set-margin('.uml',right, 20) 
set-margin('pre.source',right, 20) 
set-margin('',bottom, 20) 
set-margin('.uml',bottom, 20) 
set-margin('pre.source',bottom, 20) 
set-margin('.level2',bottom, 20) 

set-padding(left, 5)
.padding-left-0
  ul
    padding-left 0

set-position(top, 20) 
set-position(right, 20) 
set-position(left, 20) 
set-position(bottom, 20) 

.grid
  display: grid;
  column-gap: 5%
  row-gap: 20%
  align-items: start
  &.no-gap
    column-gap 0
    row-gap 0
  &.low-gap
    column-gap: 1%
    row-gap: 5%
  &.col-2
    grid-template-columns repeat(2, 1fr)
  &.col-3
    grid-template-columns repeat(3, 1fr)
  &.col-4
    grid-template-columns repeat(4, 1fr)
  &.col-2-third
    grid-template-columns 2fr 1fr

.align-top
  align-items flex-start
.align-bottom
  align-items flex-end

.full-screen
  position absolute
  z-index -1
  top 0
  left 0
  width 100%
  height 100%
  img
    width 100%
    height 100%
    object-fit contain

.background
  position absolute
  z-index -1
.foreground
  position absolute
  z-index 1

/* x2 pour assurer la compatibilité avec l'ancienne technique (en em) */
.right
  &-0
    position absolute
    right 0
  &-1
    position absolute
    right 3rem
  &-2
    position absolute
    right 6rem
  &-3
    position absolute
    right 9rem
