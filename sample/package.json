{
  "name": "sample",
  "version": "1.1.0",
  "scripts": {
    "build": "gulp build",
    "start": "gulp serve",
    "gulp": "gulp",
    "print": "print-slides --bc_course=$npm_package_name"
  },
  "config": {
    "port": 8008,
    "ratio": "4:3"
  },
  "dependencies": {
    "@sewatech/bespoke-course": "file:../module"
  },
  "devDependencies": {
    "gulp": "4.x"
  }
}
