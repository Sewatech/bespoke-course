{
  "name": "sample",
  "version": "1.0.0",
  "scripts": {
    "build": "gulp build",
    "start": "gulp serve",
    "print": "print-slides --bc_course=$npm_package_name"
  },
  "config": {
    "port": 8008,
    "ratio": "4:3"
  },
  "dependencies": {
    "@sewatech/bespoke-course": "^0.3.0"
  },
  "devDependencies": {
    "gulp": "4.x"
  }
}
